{
  "repo": "Now In Android",
  "url": "https://github.com/android/nowinandroid.git",
  "sha": "a49ed253d75e61a2b6ab80a8da677b57437b08eb",
  "commitDate": "2026-09-22",
  "pinRationale": "Pinned to HEAD at the time this matrix was hardened. Gradle 9.7.1 runs natively on a JDK-21 runner, so this repo exercises the 'ordinary' path (no JDK discovery fallback needed) alongside Sunflower/Signal's forced-discovery path.",
  "groundTruthMethod": "gradleVersion/androidSdkVersion were confirmed by reading build files. moduleCount is NOT simply the number of include(...) lines (35): settings.gradle.kts uses multi-segment paths like include(\":core:analytics\") and include(\":feature:foryou:api\"), and Gradle auto-creates an implicit parent project for every intermediate segment (:core, :feature, :feature:foryou, :feature:interests, :feature:bookmarks, :feature:topic, :feature:search, :feature:settings, :sync) whether or not that segment has its own include(...) line — 9 implicit parents on top of the 35 explicit includes = 44. jdkVersion and androidBuildToolsVersion/androidNdkVersion were confirmed by running `gradlezilla generate --dry-run --format json` against this exact pinned checkout (with a full local Android SDK present, since one module's configuration otherwise fails on SDK license/component auto-install unrelated to gradlezilla itself).",
  "groundTruth": {
    "gradleVersion": { "value": "9.7.1", "source": "gradle/wrapper/gradle-wrapper.properties: distributionUrl=...gradle-9.7.1-bin.zip" },
    "jdkVersion": { "value": 17, "source": "verified run: extractionMetadata.jdkVersionSource=\"agpMinimum\" — build-logic/convention/.../KotlinAndroid.kt sets sourceCompatibility/jvmTarget = JavaVersion.VERSION_11 (a bytecode-compatibility setting, not a Gradle/Kotlin toolchain declaration), so it does not drive jdkVersion; AGP 9.3.2 (gradle/libs.versions.toml androidGradlePlugin=9.3.2) requires JDK 17+, which is what resolves" },
    "androidSdkVersion": { "value": 36, "source": "build-logic/convention/.../KotlinAndroid.kt: compileSdk = 36 (hardcoded, applied to every module)" },
    "androidBuildToolsVersion": { "value": "36.0.0", "source": "verified run: not set anywhere in build-logic convention plugins or module build files; this is AGP 9.3.2's own default build-tools version (same default observed for Timber's AGP 9.4.0 and Wikipedia's AGP 9.4.1 — the AGP 9.x line defaults build-tools to match its own release train, not the project's compileSdk)" },
    "moduleCount": { "value": 44, "source": "35 explicit include(...) statements in settings.gradle.kts + 9 implicit parent projects created by multi-segment paths (:core, :feature, :feature:foryou, :feature:interests, :feature:bookmarks, :feature:topic, :feature:search, :feature:settings, :sync); build-logic is a separate included build (pluginManagement.includeBuild) and is not one of these. Confirmed by verified run (modules array length = 44)" },
    "ndkPresent": { "value": false, "source": "no externalNativeBuild configured — none in the module build files or in the build-logic convention plugins, and no CMakeLists.txt / Android.mk anywhere in the tree" },
    "androidNdkVersion": { "value": null, "source": "not emitted. AGP populates android.ndkVersion with its own bundled default on every project, native or not, so that field was never evidence of native code — an earlier revision of this file expected \"28.2.13676358\" here for exactly that reason. Since #38 gradlezilla emits an NDK only for modules that actually compile native code" },
    "androidCmakeVersion": { "value": null, "source": "not emitted: no module uses externalNativeBuild.cmake" }
  }
}
